{
  "name": "bw-shipment-parser",
  "version": "1.0.0",
  "description": "Serverless parser for 4flow/BorgWarner shipment PDFs, deployed on Vercel.",
  "license": "MIT",
  "scripts": {
    "dev": "vercel dev",
    "start": "vercel dev",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {
    "cheerio": "^1.0.0"
  },
  "devDependencies": {
    "@vercel/node": "^5.0.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": "20.x"
  }
}
